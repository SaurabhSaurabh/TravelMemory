services:
  backend:
    container_name: node-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
      
    ports:
      - "3001:3001"
    environment:
      MONGO_URI: mongodb+srv://sumansaurabh123_db_user:pass1008@cluster0.s22crxw.mongodb.net/travelmemory
    restart: always
    networks:
      - mynetwork

  frontend:
    container_name: react-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_BACKEND_URL: http://43.205.233.111:3001
    ports:
      - "80:80"
    #environment:
      #REACT_APP_BACKEND_URL: http://backend:3001
    depends_on:
      - backend
    restart: always
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
